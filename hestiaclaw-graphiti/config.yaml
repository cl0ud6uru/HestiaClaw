name: HestiaClaw Graphiti
version: "2.0.7"
slug: hestiaclaw_graphiti
description: Graphiti MCP server with built-in Neo4j. One add-on for long-term memory — no separate Neo4j add-on needed.
url: https://github.com/cl0ud6uru/hestiaclaw
arch:
  - amd64
  - aarch64

startup: services
boot: auto
init: false
timeout: 300

host_network: true

map:
  - data:rw

options:
  password: "changeme"
  reset_neo4j_data: false
  llm_provider: "openai"
  llm_model: "gpt-5-mini"
  openai_api_key: ""
  anthropic_api_key: ""

schema:
  password: password
  reset_neo4j_data: bool
  llm_provider: list(openai|anthropic)
  llm_model: str
  openai_api_key: password
  anthropic_api_key: password
