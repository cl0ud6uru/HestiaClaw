name: HestiaClaw Neo4j
version: "1.0.0"
slug: hestiaclaw_neo4j
description: Neo4j Community Edition with GDS plugin for HestiaClaw knowledge graph
url: https://github.com/cl0ud6uru/hestiaclaw
arch:
  - amd64
  - aarch64

startup: services
boot: auto
init: false
timeout: 120

host_network: true

map:
  - data:rw

options:
  password: "changeme"

schema:
  password: password
